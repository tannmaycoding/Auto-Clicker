# Auto-Clicker
This is an auto clicker which can click left or right mouse button on a given delay.

The working:
1. It takes the delay from user
2. It takes the mouse  button to click
3. If you press `a`: starts clicking \
   If you press `b`: stops clicking
